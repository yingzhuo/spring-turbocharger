plugins {
	id 'java-platform'
	id 'signing'
	id 'maven-publish'
}

dependencies {
	constraints {
		rootProject.subprojects.forEach { subproject ->
			if (subproject.name.startsWith('spring-turbo-')) {
				api(subproject)
			}
		}
	}
}

publishing {

	publications {
		mavenJava(MavenPublication) {
			from components.javaPlatform

			groupId = 'com.github.yingzhuo'
			artifactId = "${project.name}"
			version = "${rootProject.version}"

			pom {
				name = "${project.name}"
				description = 'Bill of Materials SpringTurbocharger'
				url = 'https://github.com/yingzhuo/spring-turbocharger'
				inceptionYear = '2025'

				licenses {
					license {
						name = 'Apache License, Version 2.0'
						url = 'https://www.apache.org/licenses/LICENSE-2.0.txt'
					}
				}

				developers {
					developer {
						id = 'yingzhuo'
						name = '应卓'
						email = 'yingzhor@gmail.com'
						roles = ['author']
						timezone = 'GMT+0800'
						url = 'https://github.com/yingzhuo'
					}
				}

				scm {
					url = 'git@github.com:yingzhuo/spring-turbocharger.git'
					connection = 'scm:git:git@github.com:yingzhuo/spring-turbocharger.git'
					developerConnection = 'scm:git:git@github.com:yingzhuo/spring-turbocharger.git'
				}

				issueManagement {
					system = 'GitHub Issues'
					url = 'https://github.com/yingzhuo/spring-turbocharger/issues'
				}
			}
		}
	}

	repositories {
		maven {
			name = 'OSSRH'
			url = rootProject.version.endsWith('SNAPSHOT') ?
				providers.gradleProperty('project.publishing.ossrhSnapshotUrl').getOrNull():
				providers.gradleProperty('project.publishing.ossrhReleaseUrl').getOrNull()

			credentials {
				username = providers.gradleProperty('ossrh.token.username').getOrNull()
				password = providers.gradleProperty('ossrh.token.password').getOrNull()
			}
		}
	}
}

signing {
	useGpgCmd()
	sign publishing.publications.mavenJava
}

tasks.withType(GenerateModuleMetadata).configureEach {
	enabled = false
}
