pluginManagement {
	evaluate(file("config/maven/RepositoriesSupport.groovy")).applyPluginManagement(this)
}

dependencyResolutionManagement {
	versionCatalogs {
		create('libs', { from(files('../gradle/libs.versions.toml')) })
	}

	evaluate(file("config/maven/RepositoriesSupport.groovy")).applyDependencyResolutionManagement(this)

	repositoriesMode = RepositoriesMode.FAIL_ON_PROJECT_REPOS
}
