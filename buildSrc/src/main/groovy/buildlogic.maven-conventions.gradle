plugins {
	id 'signing'
	id 'maven-publish'
}

publishing {
	publications {
		mavenJava(MavenPublication) {
			from components.java

			groupId = "$project.group"
			artifactId = project.name
			version = project.version

			versionMapping {
				usage('java-api') {
					fromResolutionOf('runtimeClasspath')
				}
				usage('java-runtime') {
					fromResolutionResult()
				}
			}

			pom {
				name = "${project.name}"
				description = 'Another enhancement library of SpringBoot & SpringFramework'
				url = 'https://github.com/yingzhuo/spring-turbocharger'
				inceptionYear = '2025'

				licenses {
					license {
						name = 'Apache License, Version 2.0'
						url = 'https://www.apache.org/licenses/LICENSE-2.0.txt'
					}
				}

				developers {
					developer {
						id = 'yingzhuo'
						name = '应卓'
						email = 'yingzhor@gmail.com'
						roles = ['author']
						url = 'https://github.com/yingzhuo'
					}
				}

				scm {
					url = 'git@github.com:yingzhuo/spring-turbocharger.git'
					connection = 'scm:git:git@github.com:yingzhuo/spring-turbocharger.git'
					developerConnection = 'scm:git:git@github.com:yingzhuo/spring-turbocharger.git'
				}

				issueManagement {
					system = 'GitHub Issues'
					url = 'https://github.com/yingzhuo/spring-turbocharger/issues'
				}
			}
		}
	}

	repositories {
		maven {
			name = 'OSSRH'
			url = project.version.endsWith('SNAPSHOT') ?
				'https://oss.sonatype.org/content/repositories/snapshots/' :
				'https://oss.sonatype.org/service/local/staging/deploy/maven2/'

			credentials {
				// 私密信息在 $GRADLE_USER_HOME/gradle.properties
				username = SharedFunctions.getPropertyOrEmpty(project, 'ossrhTokenUsername')
				password = SharedFunctions.getPropertyOrEmpty(project, 'ossrhTokenPassword')
			}
		}
	}
}

signing {
	useGpgCmd()
	sign publishing.publications.mavenJava
}

tasks.withType(GenerateModuleMetadata).configureEach {
	enabled = false
}
